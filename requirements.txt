# ????????????????????????????????????????????????????????????????????????????
# RAGent - 장기 기억 시스템 (Vector DB + RAG)
# requirements.txt
# ????????????????????????????????????????????????????????????????????????????

# 핵심 데이터 처리
numpy>=1.24.0
pandas>=1.5.0

# Dense 임베딩 (all-MiniLM-L6-v2)
sentence-transformers>=2.2.0
torch>=2.0.0

# Vector Database (Qdrant)
qdrant-client>=2.4.0

# Sparse 임베딩 (BM25 - 신규)
rank-bm25>=0.2.1

# 텍스트 처리
python-dotenv>=0.20.0

# 선택사항 (나중에)
# pinecone-client>=2.2.0  # Pinecone 통합 시
# redis>=4.5.0            # Redis 캐싱 시
